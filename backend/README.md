# Blockchain Certificate Verifier Backend

This is the backend service for the Blockchain Certificate Verifier application.

## Setup

1. Install dependencies:
   ```bash
   poetry install
   ```

2. Run the server:
   ```bash
   poetry run uvicorn app.main:app --reload
   ```
